name: Update group functionality
on: [push]
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/update_groups.py
      - name: Upload updated index
        uses: actions/upload-artifact@v4
        with:
          name: updated-index
          path: index.html
